server:
  port: 18081
eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
     # defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
      #defaultZone: http://www.eureka02.com:18082/eureka/
      defaultZone: http://127.0.0.1:18082/eureka/
